import React from 'react';
import { Box } from '@bigcommerce/big-design';

const Header = () => {
    return (
        <Box marginBottom="xxLarge">
            {/* You can add a title or any other static content here if needed */}
        </Box>
    );
};

export default Header;
